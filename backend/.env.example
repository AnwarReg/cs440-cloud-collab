# Backend Environment Variables
PORT=5001
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# MySQL Connection (Option 1: Single Connection URI, e.g. for Railway)
# DATABASE_URL=mysql://root:password@localhost:3306/cs440_collab

# MySQL Connection (Option 2: Individual Connection Parameters)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=cs440_collab
